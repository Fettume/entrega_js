import React from 'react';

const ItemListContainer = ({ message }) => {
  return (
    <div>
      <h2>{message}</h2>
      {/* Aquí iría la lógica para mostrar los ítems */}
    </div>
  );
};

export default ItemListContainer;
